import 'jest';

describe("sample test suite 5", () => {
   
    
    describe('Sample Fixture 1', () => {
        it('should do this and that 1', () => {
            
        })
        it('should do this and that 2', () => {
            
        })
        it('should do this and that 3', () => {
            
        })
    });
    describe('Sample Fixture 2', () => {
        it('should do this and that 1', () => {
            
        })
        it('should do this and that 2', () => {
            
        })
        it('should do this and that 3', () => {
            
        })
    });
});